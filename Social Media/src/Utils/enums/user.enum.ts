
export enum Genderenum{
    Male = "Male",
    Female  = "Female",
}

export enum Roleenum{
    Admin = "Admin",
    User  = "User",
}

export enum tokentypeEnum{
    access = "access",
    refresh = "refresh",
}